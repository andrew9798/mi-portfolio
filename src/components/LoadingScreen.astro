---
// Componente LoadingScreen con logo placeholder
---

<div id="loading-screen" class="fixed inset-0 bg-light flex flex-col items-center justify-center z-[9999] transition-opacity duration-500 opacity-100">
  <div class=" mb-8 flex items-center justify-center  text-white text-2xl font-bold animate-pulse-slow">
    <img src="/logoPrincipal.png" alt="Logo" width="300px"/>
  </div>
  <div class="flex space-x-2">
    <div class="dot animate-bounce-1 bg-primary rounded-full w-4 h-4"></div>
    <div class="dot animate-bounce-2 bg-primary rounded-full w-4 h-4"></div>
    <div class="dot animate-bounce-3 bg-primary rounded-full w-4 h-4"></div>
    <div class="dot animate-bounce-4 bg-primary rounded-full w-4 h-4"></div>
  </div>
</div>

<style>
  @keyframes pulse-slow {
    0%, 100% {
      transform: scale(1);
      opacity: 1;
    }
    50% {
      transform: scale(1);
      opacity: 0.8;
    }
  }

  @keyframes bounce-1 {
    0%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
  }

  @keyframes bounce-2 {
    0%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
  }

  @keyframes bounce-3 {
    0%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
  }
  
  @keyframes bounce-4 {
    0%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 2s infinite ease-in-out;
  }

  .animate-bounce-1 {
    animation: bounce-1 1.4s infinite ease-in-out;
  }

  .animate-bounce-2 {
    animation: bounce-2 1.4s infinite ease-in-out 0.2s;
  }

  .animate-bounce-3 {
    animation: bounce-3 1.4s infinite ease-in-out 0.4s;
  }

  .animate-bounce-4 {
    animation: bounce-4 1.4s infinite ease-in-out 0.6s;
  }
</style>
